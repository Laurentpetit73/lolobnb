{% extends 'base.html.twig' %}


{% block title %}-Nos Anoonces{% endblock %}

{% block body %}

<div class="container-fluid ban p-3" style="background-image: url({{ad.coverImage}}); min-height:350px">
    <div class="container">
        <h1 class="text-center">{{ad.title}}</h1>
        <p>{{ad.introduction | raw}}</p>
        <p class="h2">
            <strong>{{ad.rooms}} chambres</strong> pour <strong>{{ad.price}} euro</strong> par nuit
        
        </p>
        <a href="#" class="btn btn-primary">Reserver</a>

    </div>
</div>
<div class="container mb-5">
    <div class="row">
        <div class="col-md-8 mt-3">
            <p class="h2">{{ad.content | raw}}</p>
            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                <ol class="carousel-indicators">
                    {% for image in ad.images %}
                    <li data-target="#carouselExampleIndicators" data-slide-to="{{loop.index0}}" {% if loop.first %} class="active" {% endif %}></li>
                    {% endfor %} 
                </ol>
                <div class="carousel-inner">
                    {% for image in ad.images %}
                    <div class="carousel-item {% if loop.first %} active {% endif %}">
                        <img class="d-block w-100" src="{{image.url}}" alt="{{image.caption}}">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>{{image.caption}}</h5>
                        </div>
                    </div>
                    {% endfor %}     
                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
                </div>
        </div>
        <div class="col">
            <div class="row my-3">
                <div class="col-4 ">
                    <img src="http://placehold.it/64x64" alt="avatar de joseph">
                </div>
                <div class="col">
                    <h3>Laurent petit</h3>
                    <span class="badge badge-primary">3 annonces</span>  
                </div>
            </div>
            <p>Lorem ipsum dolor, sit amet consectetur adipisicing elit. Nobis esse totam earum consequatur quae nam mollitia at iste laboriosam neque dicta, recusandae sapiente architecto nihil fugiat incidunt quidem voluptates? Nihil et obcaecati provident quas sit minus repellendus impedit! Suscipit, cum!</p>
        </div>
    </div>
    
</div>
{% endblock %}
